# Ignora build, dependências, dados e SEGREDOS na raiz do repo (todas as pastas).
node_modules/
.next/
out/
build/
dist/

# Dados de cliente / runtime (NUNCA versionar)
data/
**/data/

# Segredos (NUNCA versionar)
.env
.env.*
**/.env
**/.env.*

# Misc
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
.DS_Store
SECURITY_REVIEW*
